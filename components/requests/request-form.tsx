"use client";
import { useState } from "react";
export function RequestForm() { const [status,setStatus] = useState(""); async function submit(event:React.FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); setStatus("Submitting your request…"); const response = await fetch("/api/school-requests", {method:"POST", headers:{"content-type":"application/json"}, body:JSON.stringify(Object.fromEntries(form))}); const body = await response.json(); setStatus(response.ok ? `Request received: ${body.reference}` : body.error); } return <form className="card stack" onSubmit={submit}><label>School name<input required name="schoolName" /></label><label>School address<textarea required name="address" /></label><label>Your name<input required name="contactName" /></label><label>Email<input required type="email" name="email" /></label><label>Title ISBN<input required name="isbn" /></label><label>Copies<input required min="1" type="number" name="quantity" /></label><label className="skip">Leave blank<input name="website" tabIndex={-1} autoComplete="off" /></label><button className="button">Reserve requested copies</button><p role="status" aria-live="polite">{status}</p></form>; }
